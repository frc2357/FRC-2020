package com.systemmeltdown.robotlog.subsystems;

import com.systemmeltdown.robot.subsystems.IntakeSubsystem; // Imported for javadoc (for now)

/**
 * A class for logging the commands used by the {@link IntakeSubsystem}.
 * 
 * @category Intake
 * 
 * @category Logging
 */
class IntakeLogger {
    
}